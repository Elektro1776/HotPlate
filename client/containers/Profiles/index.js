import React, { Component } from 'react';
import { connect } from 'react-redux';
import { Link } from 'react-router-dom';

// Components

// Dummy data

/**
|--------------------------------------------------
Sales Profile	"Name
address
SSN
W9"	contact method phone or email for sales rep for ?'s
Videos	"easy access to videos
media tips"	blog post template
Dealer	"Create Profile
Profile - Pending
Approval"	form ability to have user roles owner, manager, sales-person, ? 
|--------------------------------------------------
*/

class Profile extends Component {
  state = {
  }
  render() {
    return (
      
    );
  }
}

export default connect((state) => ({
}), (dispatch) => ({
}))(Profile);
